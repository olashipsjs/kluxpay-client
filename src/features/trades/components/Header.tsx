import Heading from '@components/base/heading/Heading';
import React from 'react';

const Header = () => {
  return (
    <React.Fragment>
      <Heading mb={24}>Trades</Heading>
    </React.Fragment>
  );
};

export default Header;
